# Local Models

All models are local and intentionally small. No binaries are included in this repository.

- Speech-to-text should use a small, local Whisper-style model.
- Mutation uses a local llama.cpp-style LLM runner.

The goal is constrained, visible nondeterminism rather than accuracy.
