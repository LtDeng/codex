export async function captureAudio() {
  // Placeholder for microphone capture.
  // This intentionally truncates sessions, injects latency, and discards chunks.
  // Hostility is a feature: users must repeat themselves and adapt.
  return {
    audioBuffer: null,
    note: "Microphone capture is not implemented. Expect loss and delay."
  };
}
